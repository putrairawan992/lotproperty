
  # LotProperty Agent Platform

  This is a code bundle for LotProperty Agent Platform. The original project is available at https://www.figma.com/design/n2cvmBuicoE0aIkMGWULih/LotProperty-Agent-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  